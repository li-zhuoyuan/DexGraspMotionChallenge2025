# Note for DexGrasp
* Add the git repo into my submodule, you can use it directly or just delete it and clone a new one, just like **install.sh** or **env_install.sh**
I add a camera to log the experiment. If you need to run it on a server, it will be helpful.
You can watch the result in the **output_env.mp4**
